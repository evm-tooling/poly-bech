[project]
name = "test-demo"
version = "0.1.0"

[defaults]
iterations = 1000
warmup = 100
languages = [
    "go",
    "ts",
]

[go]
module = "test-demo"
version = "1.21"

[ts]
runtime = "node"

[output]
report_formats = []
output_dir = "out"
auto_save_results = true
